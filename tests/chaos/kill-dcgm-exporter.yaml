apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kill-dcgm-exporter
  namespace: hopper-system
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces: [gpu-operator]
    labelSelectors:
      app: nvidia-dcgm-exporter

