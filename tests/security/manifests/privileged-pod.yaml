apiVersion: v1
kind: Pod
metadata:
  name: security-privileged-attempt
  namespace: hopper
spec:
  restartPolicy: Never
  containers:
    - name: attempt
      image: busybox:1.36
      command: ["sleep", "30"]
      securityContext:
        privileged: true

