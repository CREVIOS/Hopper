apiVersion: v1
kind: Pod
metadata:
  name: security-hostnetwork-attempt
  namespace: hopper
spec:
  hostNetwork: true
  restartPolicy: Never
  containers:
    - name: attempt
      image: busybox:1.36
      command: ["sleep", "30"]

